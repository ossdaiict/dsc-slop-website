import { animated } from 'react-spring'
import styled, { createGlobalStyle } from 'styled-components'

const ContainerStyle = styled(animated.div)`
`

const Item = styled(animated.div)` 
`

export {ContainerStyle, Item }
